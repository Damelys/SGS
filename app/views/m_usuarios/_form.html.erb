<%= form_with(model: m_usuario, local: true) do |form| %>
  <% if m_usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_usuario.errors.count, "error") %> prohibited this m_usuario from being saved:</h2>

      <ul>
      <% m_usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
