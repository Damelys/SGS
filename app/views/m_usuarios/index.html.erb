<p id="notice"><%= notice %></p>

<h1>M Usuarios</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @m_usuarios.each do |m_usuario| %>
      <tr>
        <td><%= link_to 'Show', m_usuario %></td>
        <td><%= link_to 'Edit', edit_m_usuario_path(m_usuario) %></td>
        <td><%= link_to 'Destroy', m_usuario, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New M Usuario', new_m_usuario_path %>
