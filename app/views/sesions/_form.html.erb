<%= form_with(model: sesion, local: true) do |form| %>
  <% if sesion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sesion.errors.count, "error") %> prohibited this sesion from being saved:</h2>

      <ul>
      <% sesion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
