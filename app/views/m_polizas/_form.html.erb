<%= form_with(model: m_poliza, local: true) do |form| %>
  <% if m_poliza.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(m_poliza.errors.count, "error") %> prohibited this m_poliza from being saved:</h2>

      <ul>
      <% m_poliza.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
