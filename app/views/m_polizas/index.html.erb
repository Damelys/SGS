<p id="notice"><%= notice %></p>

<h1>M Polizas</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @m_polizas.each do |m_poliza| %>
      <tr>
        <td><%= link_to 'Show', m_poliza %></td>
        <td><%= link_to 'Edit', edit_m_poliza_path(m_poliza) %></td>
        <td><%= link_to 'Destroy', m_poliza, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New M Poliza', new_m_poliza_path %>
