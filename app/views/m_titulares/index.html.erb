<p id="notice"><%= notice %></p>

<h1>M Titulares</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @m_titulares.each do |m_titular| %>
      <tr>
        <td><%= link_to 'Show', m_titular %></td>
        <td><%= link_to 'Edit', edit_m_titular_path(m_titular) %></td>
        <td><%= link_to 'Destroy', m_titular, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New M Titular', new_m_titular_path %>
